# shinsaku-calendar

## プロジェクト概要

**FANZA の発売・配信予定と、いま割引されているものを、日付から引くカレンダー。**

darekore.jp（`darenano`）の作り直しではない。**別のものを作る。**
2026-09-12 に darekore.jp で分かったことを設計に落としてある。理由は README に書いた。

本番URL: 未定。独自ドメインが決まったら、リポジトリの **Variables に `SITE_DOMAIN`** を入れる。
それだけで CNAME・canonical・サイトマップ・OGP・連絡先が切り替わる。**個別のファイルを書き換えない。**

## 守ること

### 1. ページ数を3桁に抑える

darekore.jp は 59,445ページ作って、21,147件を Google が取りに来なかった。
**作品1本につき1ページを作らない。** ジャンル・レーベルは
`MIN_GENRE` / `MIN_LABEL` 本以上あるものだけページにする。

### 2. アダルトでないクエリを狙わない

アダルト判定は覆せない（2026-09-11 に実測。セーフサーチ Filter で `site:darekore.jp`
が1件しか返らなかった）。**人名で拾おうとしない。**
「9月に出る新作」「いま割引されているもの」を探す人に向ける。

### 3. 承認が下りるまでアフィリエイトIDを付けない

参加規約 第7条「当社に申請していないサイトにおけるアフィリエイトIDの利用行為」は
禁止行為で、罰則は**事前の通知なく報酬の没収**。
`FANZA_AFFILIATE_ID` が空なら素の作品URLで作る。これが**承認前の正しい状態**。

承認後に Secrets へIDを入れて取り直すと、API が `affiliateURL` を返すので切り替わる。
**URLを手で書き換えない**（提供素材の改変にあたる）。

### 4. 画像を切り抜かない

DMM の画像利用の基本ルールは「拡大・縮小のみ可。その他の加工はご遠慮ください」。
CSS は `object-fit: contain`。**`cover` にしない。**
2026-09-09 にグラビア名鑑がこれで審査に落ちている。

### 5. 実在の人物を扱っている

- **出典で確認できないことは書かない。** 推測・補完・独自の評価を載せない
- 「お得」「激安」のような評価の言葉を使わない。**数字だけを出す**
- 削除依頼の窓口を画面に出す
- 18歳未満に向けたものでない旨を出す

### 6. 利用者の投稿機能を置かない

darekore.jp では、運営が自分で口コミを書き、投票していた。
景品表示法第5条第3号（ステマ規制）に当たる。
**動いていることは、発売予定という実データの更新で示す。**

### 7. 公開したURLを落とさない

月別ページは消さない。取得が0件だった月は、前に取ったぶんを残す
（`fetch-schedule.py` がそう書いてある）。

## データ取得

| 出典 | スクリプト | 環境変数 |
|---|---|---|
| DMM.com アフィリエイト Web サービス（FANZA digital/videoa） | `scripts/fetch-schedule.py` | `FANZA_API_ID`（必須）/ `FANZA_AFFILIATE_ID`（承認後） |

`data/campaigns.json` は**手で書く**。DMM の公式メルマガ・公式ページに
書いてあったキャンペーンだけを入れる。期間（`to`）が過ぎたものは自動で表示から外れる。

**キーはリポジトリに書かない。** GitHub Secrets に入れる。

## 主なコマンド

- `python scripts/fetch-schedule.py` — 発売予定を取る（要 `FANZA_API_ID`）
- `node scripts/build-site.mjs` — `dist/` にサイトを組み立てる

## デプロイ

`.github/workflows/deploy.yml` が `main` への push でビルド＆デプロイ。
`fetch.yml` が毎日 05:00 JST に取り直し、**変わったときだけ**コミットしてデプロイを呼ぶ。
